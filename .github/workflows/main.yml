name: CI/CD

on:
  push:
    branches:
      - "dev"
      - "main"
      - "feat/cicd/docker2"
  workflow_dispatch: # 수동 트리거
    inputs:
      environment:
        description: 'Deploy environment'
        required: true
        default: 'dev'

permissions:
  contents: write
  packages: write
  actions: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 레포지토리 코드 체크아웃
      - name: Checkout Code
        uses: actions/checkout@v3

      # 2️⃣ JDK 17 설정
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'zulu'

      # 3️⃣ Gradle 의존성 캐시
      - name: Cache Gradle dependencies
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # 4️⃣ Gradle Wrapper 실행 권한 부여
      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      # 5️⃣ 테스트 환경 변수 설정
      - name: Set environment variables for tests
        run: |
          cp .env.test .env  # 테스트 환경용 .env 파일 복사

      # 6️⃣ Gradle 빌드 및 테스트
      - name: Build with Gradle
        run: ./gradlew build -x test

      - name: Test with Gradle
        run: ./gradlew test --info --stacktrace

      # # 7️⃣ DockerHub 로그인
      # - name: DockerHub Login
      #   if: success() && (github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/feat/cicd/docker2')
      #   run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      # # 8️⃣ Docker 이미지 빌드 및 푸시
      # - name: Build and Push Docker Images
      #   if: success() && github.ref == 'refs/heads/main'
      #   run: |
      #     IMAGE_TAG=$(echo $GITHUB_SHA | cut -c1-7)
      #     docker-compose -f docker-compose.yml build --build-arg VERSION=$IMAGE_TAG  # 전체 서비스 빌드
      #     docker-compose -f docker-compose.yml push  # 전체 서비스 푸시

      # # 🔟 서버에 배포
      # - name: Deploy to server
      #   if: success() && (github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/feat/cicd/docker2')
      #   uses: appleboy/ssh-action@v1.1.0
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ec2-user
      #     key: ${{ secrets.SSH_KEY }}
      #     port: 22
      #     script: |
      #       cd /home/ec2-user/flight-booking  # EC2 서버의 프로젝트 경로로 이동
      #       cp .env.prod .env  # 운영 환경용 .env 파일 복사

      #       # 서비스 순차 실행
      #       SERVICES=("eureka-server" "config-server" "gateway-service" "notification-service" "user-service" "booking-service" "payment-service" "flight-service" "ticket-service")

      #       for SERVICE in "${SERVICES[@]}"; do
      #         echo "Starting $SERVICE..."
      #         docker-compose pull $SERVICE
      #         docker-compose up -d --build $SERVICE
      #         sleep 30  # 각 서비스 실행 후 대기 시간 (필요시 조정 가능)
      #       done

      #       docker image prune -af  # 불필요한 이미지 정리
      #       docker ps  # 실행 중인 컨테이너 확인
