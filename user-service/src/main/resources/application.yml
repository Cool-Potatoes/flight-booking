server:
  port: 0  # User 서비스 고정 포트

spring:
  application:
    name: user-service
  config:
    import: "optional:configserver:"  # Config Server에서 설정 불러오기 (Config Server가 없어도 실행 가능)
  cloud:
    config:
      discovery:
        enabled: true  # Eureka를 통해 Config Server 검색
        service-id: config-server

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus  # Actuator 헬스 체크 및 모니터링

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    hostname: ${HOSTNAME}
    prefer-ip-address: true  # IP 기반 서비스 등록
