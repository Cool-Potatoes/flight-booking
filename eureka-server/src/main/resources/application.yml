server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false   # Eureka 서버는 자신을 등록하지 않음
    fetch-registry: false         # 다른 서비스 정보를 조회하지 않음
  instance:
    prefer-ip-address: true       # IP 주소로 등록
